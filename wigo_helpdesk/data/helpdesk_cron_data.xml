<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Acción programada: verificar SLA vencidos cada hora (Odoo 19) -->
        <record id="ir_cron_check_sla_deadline" model="ir.cron">
            <field name="name">Helpdesk WigoFast: Verificar SLA vencidos</field>
            <field name="model_id" ref="wigo_helpdesk.model_helpdesk_ticket"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_sla_deadline()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active">True</field>
        </record>
    </data>
</odoo>
